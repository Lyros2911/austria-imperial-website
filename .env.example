# ==============================================
# Austria Imperial Green Gold - Environment
# ==============================================

# Database (Neon Postgres)
POSTGRES_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Stripe (same account as Auryx AI LLC)
STRIPE_SECRET_KEY=sk_test_***
STRIPE_WEBHOOK_SECRET=whsec_***

# App
BASE_URL=http://localhost:3000
AUTH_SECRET=generate-a-random-64-char-hex-string

# ─── Producer APIs ───────────────────────────
# Kiendler (Kernöl) — API mode (optional, Email fallback if empty)
KIENDLER_API_URL=
KIENDLER_API_KEY=
KIENDLER_EMAIL=bestellung@kiendler.at

# Hernach (Kren) — API mode (optional, Email fallback if empty)
HERNACH_API_URL=
HERNACH_API_KEY=
HERNACH_EMAIL=bestellung@hernach.at

# ─── SMTP (for producer email fallback) ──────
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=bestellungen@austria-imperial.com
